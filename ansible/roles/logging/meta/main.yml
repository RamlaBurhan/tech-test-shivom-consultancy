---
galaxy_info:
  role_name: logging
  author: platform-team
  description: Deploys(ELK) as a Docker Compose stack
  license: MIT
  min_ansible_version: "2.15"
  platforms:
    - name: Ubuntu
      versions:
        - jammy
        - noble

dependencies:
  - role: common
  - role: docker
